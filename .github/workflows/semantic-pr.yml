# DO NOT EDIT — managed by hugoh/go-tools via copier.
---
name: semantic-pr

on: # yamllint disable-line rule:truthy
  pull_request:
    types: [opened, edited, reopened, synchronize]

permissions: {}

jobs:
  semantic-pr:
    uses: hugoh/go-tools/.github/workflows/semantic-pr.yml@e3f077fff3425c612d2a0e8a8682715d9a172cc9 # v1.26.0
    permissions:
      pull-requests: read
