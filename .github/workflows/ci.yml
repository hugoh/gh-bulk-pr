# DO NOT EDIT — managed by hugoh/go-tools via copier.
---
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      bump_type:
        type: choice
        description: "Version bump type"
        default: auto
        options: [auto, patch, minor, major]

permissions: {}

jobs:
  hk:
    uses: hugoh/go-tools/.github/workflows/go-hk.yml@bda3257007b9959f6cf0929b31f2e677c7ab7d21 # v1.26.3
    permissions:
      contents: read

  goci:
    uses: hugoh/go-tools/.github/workflows/go-ci.yml@bda3257007b9959f6cf0929b31f2e677c7ab7d21 # v1.26.3
    permissions:
      contents: read

  release:
    needs: [hk, goci]
    uses: hugoh/go-tools/.github/workflows/go-release.yml@bda3257007b9959f6cf0929b31f2e677c7ab7d21 # v1.26.3
    permissions:
      contents: write
    with:
      bump_type: ${{ inputs.bump_type || 'auto' }}
      has_goreleaser: true
    secrets:
      gh_token: ${{ secrets.GITHUB_TOKEN }}
      tap_github_token: ${{ secrets.TAP_GITHUB_TOKEN }}
